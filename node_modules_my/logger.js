var winston = require('winston')
var mixIn = require('mout/object/mixIn')

// logger defaults options
var defaults = {
    transports: [
      new (winston.transports.Console)(),
      new (winston.transports.File)({ filename: 'log/restapidemo.log' })
    ],
    exitOnError: false 
  }

// singleton
var  logger

var createLogger = function createLogger() {
    var opts;
    
    if (logger) {
      return logger
    }

    //opts = mixIn({}, defaults, options)
    logger = new winston.Logger(defaults)
    
    return logger
  }

module.exports = createLogger;